services:
  app:
    build: .
    ports:
      - "127.0.0.1:8086:3000"
    environment:
      - API_SECRET=${API_SECRET:-}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
